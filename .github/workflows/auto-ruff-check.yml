name: Auto Ruff Check

on:
  pull_request:
    branches: [main, dev, pyamares-xmris]
  push:
    branches: [main, dev, pyamares-xmris]

jobs:
  ruff-check:
    uses: ./.github/workflows/ruff_check.yml
